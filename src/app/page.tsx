export default async function Home() {
    // console.log(new URL(import.meta.url.pa))
    // console.log(await fs.readFile(path.join(new URL(import.meta.url).pathname, '../', 'index.html'), 'utf8'))
    return (
        <div>
            Hello world
        </div>
    )
}
